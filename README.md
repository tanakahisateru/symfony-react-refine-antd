# Symfony API Platform + refine (React) demo

- Symfony https://symfony.com/
- API Platform https://api-platform.com/
- refine https://refine.dev/

```sh
composer install
yarn install
yarn dev
symfony server:start
```

Open `/admin` `/api` `/refine` in your browser.
